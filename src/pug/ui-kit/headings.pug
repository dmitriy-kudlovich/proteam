extends ./layout/ui-layout.pug
include ./mixins/*.pug
include ./../mixins/*.pug

block variables
    - var title = 'Заголовки';
    - var pageClass = 'body--dashboard';

block content

    //-Заголовок компонента
    h1.ui-kit__component-title.
        Заголовки

    //- Демонстрация компонента
    .ui-kit__component-demo
        .container
            +section-title("Новые записи в", "main.html", "блоге")
            +section-title("Новые записи в блоге")

    //- Код для вызова компонента
    .ui-kit__component-code
        pre.
            +section-title("Новые записи в", "main.html", "блоге")
            +section-title("Новые записи в блоге")

    //- Файл в котором описан компонент
    .ui-kit__element-title.
        Название файла:
        mixins/section-title.pug

    //- Распечатка кода компонента
    .ui-kit__element-code
        pre.
            mixin section-title(text, link__ref="!#", link__text)
                if link__ref && link__text
                    h2.section-title= text + " "
                        <a class= section-title__link href=#{link__ref}>#{link__text}</a>
                else
                    h2.section-title= text
