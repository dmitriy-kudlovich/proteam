mixin notify(notify_message)
    .notify
        if block
            .notify__header&attributes(attributes)= notify_message
            .notify__text
                block
        else
            .notify__header&attributes(attributes)(style="border-radius: 6px")= notify_message

