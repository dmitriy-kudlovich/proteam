mixin header-menu(links)
    nav.header-nav
        ul.header-nav_items
            each item in links
                    li(class="header-nav_item")
                        a(class="header-nav_item" href= item.link)= item.name

                        