mixin card-post(img, text, link="#")
    .card-post
        a(href= link).card-post__img
            img(src="../img/cards/card-post/" + img, alt="post-img")
        a(href= link).card-post__text #{text}
